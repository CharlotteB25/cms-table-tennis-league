<!DOCTYPE html>
<html lang="nl">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Ploeg C</title>
    <link
      rel="icon"
      type="image/x-icon"
      href="{{ siteUrl('uploads/favicon/favicon.ico') }}"
    />

    <style>
      :root {
        --cream: #fdfaf6;
        --soft-red: #d9534f;
        --soft-red-dark: #b52e2a;
        --text: #333;
        --muted: #6b6b6b;
        --card: #fff;
        --ring: #e9e9e9;
        --shadow: 0 4px 10px rgba(0, 0, 0, 0.06);
        --tint: #fff7f5;
        --bg-gradient-start: #f2edea;
        --bg-gradient-end: #f6e0d9;
        --purple-700: #5e548e;
      }

      html {
        background: var(--bg-gradient-start);
      }
      body {
        margin: 0;
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        font-family: Arial, sans-serif;
        color: var(--text);
        overflow-x: hidden;
        position: relative;
        background: radial-gradient(
            1200px 700px at 110% -10%,
            color-mix(in oklab, var(--purple-700) 14%, transparent),
            transparent 60%
          ),
          linear-gradient(
            160deg,
            var(--bg-gradient-start),
            var(--bg-gradient-end)
          );
      }
      body::before {
        content: "";
        position: fixed;
        inset: -10% -5% auto -5%;
        height: 140%;
        background: url("data:image/svg+xml,%3Csvg width='800' height='600' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath d='M0 100 Q400 200 800 100 T1600 100 V600 H0 Z' fill='%235e548e' opacity='0.06'/%3E%3C/svg%3E")
          repeat-x center/cover;
        animation: floatBg 24s linear infinite;
        pointer-events: none;
        z-index: 0;
      }

      main {
        position: relative;
        z-index: 1;
        padding-inline: 20px;
        margin-top: 2rem;
      }
      @media (max-width: 700px) {
        main {
          padding: 1.25rem 14px;
          margin-top: 10px;
        }
      }

      .top-actions {
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: 12px;
        margin-bottom: 8px;
      }
      .btn {
        appearance: none;
        border: 2px solid var(--soft-red);
        background: #fff;
        color: var(--soft-red);
        padding: 10px 16px;
        border-radius: 10px;
        font-weight: 700;
        text-decoration: none;
        display: inline-block;
        transition: background 0.15s, color 0.15s, border-color 0.15s,
          transform 0.1s;
      }
      .btn:hover {
        background: var(--soft-red);
        color: var(--cream);
        transform: translateY(-1px);
      }

      h1 {
        margin: 10px 0 16px;
        text-align: center;
        font-size: 26px;
        font-weight: 800;
        color: var(--soft-red-dark);
      }

      /* ---------- Card grid ---------- */
      .cards {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
        gap: 14px;
      }
      @media (max-width: 760px) {
        .cards {
          grid-template-columns: 1fr;
        }
      }

      .card {
        display: block;
        background: var(--card);
        color: inherit;
        border: 1px solid var(--ring);
        border-radius: 14px;
        box-shadow: 0 8px 18px rgba(17, 12, 38, 0.06),
          0 2px 6px rgba(0, 0, 0, 0.04);
        padding: 14px;
        text-decoration: none;
        transition: transform 0.18s, box-shadow 0.18s, border-color 0.18s;
      }
      .card:hover {
        transform: translateY(-3px);
        box-shadow: 0 14px 28px rgba(17, 12, 38, 0.11),
          0 8px 16px rgba(0, 0, 0, 0.06);
        border-color: color-mix(in oklab, var(--purple-700) 18%, var(--ring));
      }

      .card__title {
        margin: 0 0 6px;
        font-weight: 800;
        color: var(--soft-red-dark);
        font-size: 1.05rem;
        line-height: 1.25;
      }
      .meta-row {
        display: grid;
        grid-template-columns: 1fr auto;
        gap: 10px;
        align-items: center;
      }
      .meta {
        color: var(--muted);
        font-size: 0.95rem;
      }
      .pill {
        justify-self: end;
        padding: 6px 10px;
        border-radius: 999px;
        border: 1px solid var(--ring);
        background: #fff;
        color: var(--soft-red-dark);
        font-weight: 700;
        font-size: 0.9rem;
      }

      .empty {
        background: var(--card);
        border: 1px solid var(--ring);
        border-radius: 12px;
        padding: 16px;
        text-align: center;
        color: var(--muted);
        box-shadow: var(--shadow);
      }
    </style>
  </head>
  <body>
    {% include 'global/nav.twig' %}

    <main>
      {% include 'global/header.twig' %}

      <div class="top-actions">
        <a href="{{ url('teams/admin') }}" class="btn">Terug</a>
      </div>

      <h1>Ploeg C</h1>

      {% set matches = craft.entries() .section('ploegC') .orderBy('dateTime
      asc, postDate asc') .all() %} {% if matches|length %}
      <div class="cards">
        {% for match in matches %} {% set dt = (match.dateTime is defined and
        match.dateTime) ? match.dateTime|date('d-m-Y H:i') : '—' %} {% set loc =
        match.address ?? match.location ?? '—' %}

        <a
          class="card"
          href="{{ match.url }}"
          aria-label="Open {{ match.title }}"
        >
          <h3 class="card__title">{{ match.title }}</h3>
          <div class="meta-row">
            <div class="meta">
              <div><strong>Datum & tijd:</strong> {{ dt }}</div>
              <div><strong>Locatie:</strong> {{ loc }}</div>
            </div>
            <span class="pill">Beschikbaarheid</span>
          </div>
        </a>
        {% endfor %}
      </div>
      {% else %}
      <p class="empty">Geen wedstrijden beschikbaar voor Ploeg C.</p>
      {% endif %}
    </main>

    {% include 'global/footer.twig' %}
  </body>
</html>
