<!DOCTYPE html>
<html lang="nl">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Ploegen (Beheer)</title>
    <link
      rel="icon"
      type="image/x-icon"
      href="{{ siteUrl('uploads/favicon_io/favicon.ico') }}"
    />

    <style>
      :root {
        --cream: #fdfaf6;
        --soft-red: #d9534f;
        --soft-red-dark: #b52e2a;
        --text: #333;
        --muted: #6b6b6b;
        --card: #ffffff;
        --ring: #e9e9e9;
        --shadow: 0 4px 10px rgba(0, 0, 0, 0.06);
        --tint: #fff7f5;
      }

      /* Page layout */
      body {
        margin: 0;
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        font-family: Arial, sans-serif;
        color: var(--text);
        background: var(--cream);
      }
      main {
        margin-left: 200px; /* space for fixed side nav */
        padding: 2rem;
        flex: 1;
      }

      /* Top bar */
      .top-actions {
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: 12px;
        margin: 10px;
      }
      .btn {
        appearance: none;
        border: 2px solid var(--soft-red);
        background: #fff;
        color: var(--soft-red);
        padding: 10px 16px;
        border-radius: 8px;
        font-weight: 700;
        text-decoration: none;
        display: inline-block;
        transition: background 0.15s ease, color 0.15s ease,
          border-color 0.15s ease, transform 0.1s ease;
      }
      .btn:hover {
        background: var(--soft-red);
        color: var(--cream);
        transform: translateY(-1px);
      }

      /* Title */
      .page-title {
        text-align: center;
        margin: 6px 0 18px;
        font-size: 24px;
        font-weight: 800;
        color: var(--soft-red-dark);
      }

      /* Team tiles */
      .team-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
        gap: 16px;
        max-width: 1100px;
        margin: 0 auto;
      }
      .team-card {
        background: var(--card);
        border: 1px solid var(--ring);
        border-radius: 12px;
        box-shadow: var(--shadow);
        width: 300px;
        height: auto;
        padding: 16px;
        text-decoration: none;
        color: inherit;
        display: grid;
        grid-template-columns: auto 1fr auto;
        gap: 12px;
        align-items: center;
        transition: transform 0.15s ease, border-color 0.15s ease,
          background 0.15s ease, box-shadow 0.15s ease;
      }
      .team-card:hover {
        transform: translateY(-2px);
        border-color: var(--soft-red);
        background: var(--tint);
        box-shadow: 0 8px 16px rgba(0, 0, 0, 0.12);
      }
      .badge {
        width: 40px;
        height: 40px;
        border-radius: 10px;
        background: #fde3e2;
        color: var(--soft-red-dark);
        display: flex;
        align-items: center;
        justify-content: center;
        font-weight: 800;
        font-size: 1.1rem;
        border: 1px solid rgba(181, 46, 42, 0.25);
      }
      .ttext {
        display: flex;
        flex-direction: column;
      }
      .ttext h2 {
        margin: 0;
        font-size: 18px;
        color: var(--soft-red-dark);
      }
      .ttext p {
        margin: 4px 0 0;
        font-size: 0.92rem;
        color: var(--muted);
      }
      .chev {
        color: var(--soft-red);
        font-weight: 800;
        font-size: 20px;
      }

      @media (max-width: 700px) {
        main {
          padding: 1.25rem;
        }
      }
    </style>
  </head>
  <body>
    {% include 'global/nav.twig' %}

    <main>
      {% include 'global/header.twig' %}

      <div class="top-actions">
        <a href="{{ url('index') }}" class="btn">Terug</a>
      </div>

      <div class="page-title">Selecteer je ploeg</div>

      <div class="team-grid">
        {% for team in ['ploegA','ploegB','ploegC'] %} {% set letter =
        team|slice(-1)|upper %}
        <a
          class="team-card"
          href="{{ url('teams/' ~ team) }}"
          aria-label="Beheer {{ team | capitalize }}"
        >
          <span class="badge">{{ letter }}</span>
          <div class="ttext">
            <h2>Beheer {{ team | capitalize }}</h2>
            <p>
              Planning en opstelling aanpassen voor {{ team | capitalize }}.
            </p>
          </div>
          <span class="chev">›</span>
        </a>
        {% endfor %}
      </div>
    </main>

    {% include 'global/footer.twig' %}
  </body>
</html>
