<!DOCTYPE html>
<html lang="nl">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Ploegen (Beheer)</title>
    <link
      rel="icon"
      type="image/x-icon"
      href="{{ siteUrl('uploads/favicon/favicon.ico') }}"
    />

    <style>
      :root {
        /* Base */
        --cream: #fdfaf6;
        --card: #ffffff;
        --ring: #e9e9e9;
        --text: #222;
        --muted: #6b6b6b;
        --shadow: 0 8px 20px rgba(17, 12, 38, 0.06);

        /* Accent red */
        --soft-red: #d9534f;
        --soft-red-dark: #b52e2a;

        /* Purple family */
        --purple-700: #5e548e;
        --purple-900: #231942;
        --purple-50: #f3effa;

        /* Background tones */
        --bg-gradient-start: #fffaf6;
        --bg-gradient-end: #f4e7e3;
      }

      /* Page layout + background */
      html {
        background-color: var(--bg-gradient-start);
      }
      body {
        margin: 0;
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        font-family: Arial, sans-serif;
        color: var(--text);
        background: radial-gradient(
            1200px 700px at 110% -10%,
            color-mix(in oklab, var(--purple-700) 14%, transparent),
            transparent 60%
          ),
          linear-gradient(
            160deg,
            var(--bg-gradient-start),
            var(--bg-gradient-end)
          );
        position: relative;
        overflow-x: hidden;
      }

      /* Subtle animated wave band */
      body::before {
        content: "";
        position: fixed;
        inset: -10% -5% auto -5%;
        height: 140%;
        z-index: 0;
        background: url("data:image/svg+xml,%3Csvg width='800' height='600' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath d='M0 100 Q400 200 800 100 T1600 100 V600 H0 Z' fill='%235e548e' opacity='0.06'/%3E%3C/svg%3E");
        background-repeat: repeat-x;
        background-size: cover;
        animation: floatBg 24s linear infinite;
        pointer-events: none;
      }

      main {
        margin-left: 200px; /* space for fixed side nav */
        flex: 1;
        position: relative;
        z-index: 1; /* above wave */
      }

      /* Top actions (kept) */
      .top-actions {
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: 12px;
        margin: 10px;
      }
      .btn {
        appearance: none;
        border: 2px solid var(--soft-red);
        background: #fff;
        color: var(--soft-red);
        padding: 10px 16px;
        border-radius: 10px;
        font-weight: 700;
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        gap: 8px;
        transition: transform 0.15s ease, background 0.15s ease,
          color 0.15s ease, border-color 0.15s ease;
      }
      .btn:hover {
        background: var(--soft-red);
        color: var(--cream);
        transform: translateY(-1px);
      }
      .btn:focus-visible {
        outline: none;
        box-shadow: 0 0 0 3px
          color-mix(in oklab, var(--purple-700) 35%, transparent);
        border-color: color-mix(
          in oklab,
          var(--purple-700) 50%,
          var(--soft-red)
        );
      }

      /* Title */
      .page-title {
        text-align: center;
        margin: 60px auto 18px;
        font-size: 26px;
        font-weight: 900;
        color: var(--soft-red-dark);
        max-width: 1100px;
      }

      /* Team grid */
      .team-grid {
        display: grid;
        grid-template-columns: repeat(
          auto-fit,
          minmax(320px, 1fr)
        ); /* bigger cards */
        gap: 40px; /* more breathing room between cards */
        max-width: 1100px; /* allow more width overall */
        margin: 60px auto 80px; /* space above/below grid */
        padding: 0 2rem; /* space at edges */
      }

      /* Card with gradient edge + subtle glow */
      .team-card {
        position: relative;
        background: var(--card);
        border: 1px solid var(--ring);
        border-radius: 20px; /* slightly rounder */
        box-shadow: 0 12px 26px rgba(17, 12, 38, 0.08),
          0 4px 12px rgba(0, 0, 0, 0.05);
        padding: 32px 28px; /* larger inner spacing */
        text-decoration: none;
        color: inherit;
        display: grid;
        grid-template-columns: auto 1fr auto;
        gap: 20px;
        align-items: center;
        transition: transform 0.18s ease, box-shadow 0.18s ease,
          border-color 0.18s ease;
        isolation: isolate;
      }

      /* gradient border edge (keep your existing ::before if you had it) */

      /* Hover / focus states */
      .team-card:hover {
        transform: translateY(-5px);
        border-color: color-mix(in oklab, var(--purple-700) 22%, var(--ring));
        box-shadow: 0 18px 36px rgba(17, 12, 38, 0.12),
          0 8px 20px rgba(0, 0, 0, 0.06);
      }

      .team-card:focus-visible {
        outline: none;
        box-shadow: 0 0 0 4px
            color-mix(in oklab, var(--purple-700) 40%, transparent),
          0 18px 36px rgba(17, 12, 38, 0.12);
      }

      .badge {
        width: 56px;
        height: 56px;
        border-radius: 1.3rem;
        background: linear-gradient(180deg, #fff, var(--purple-50));
        color: var(--purple-700);
        display: flex;
        align-items: center;
        justify-content: center;
        font-weight: 900;
        font-size: 1.1rem;
        border: 1px solid color-mix(in oklab, var(--purple-700) 24%, #fff);
        box-shadow: inset 0 0 6px rgba(0, 0, 0, 0.04);
      }

      .ttext {
        display: flex;
        flex-direction: column;
      }
      .ttext h2 {
        margin: 0;
        font-size: 1.2rem;
        color: var(--soft-red-dark);
        letter-spacing: 0.2px;
      }
      .ttext p {
        margin: 4px 0 0;
        font-size: 1rem;
        color: var(--muted);
      }

      .chev {
        color: var(--purple-700);
        font-weight: 900;
        font-size: 22px;
        line-height: 1;
        transform: translateY(-1px);
      }
    </style>
  </head>
  <body>
    {% include 'global/nav.twig' %}

    <main>
      {% include 'global/header.twig' %}

      <div class="page-title">Selecteer je ploeg</div>

      <div class="team-grid">
        {% for team in ['ploegA','ploegB','ploegC'] %} {% set letter =
        team|slice(-1)|upper %}
        <a
          class="team-card"
          href="{{ url('teams/' ~ team) }}"
          aria-label="Beheer {{ team | capitalize }}"
        >
          <span class="badge">{{ letter }}</span>
          <div class="ttext">
            <h2>Beheer {{ team | capitalize }}</h2>
            <p>
              Planning en opstelling aanpassen voor {{ team | capitalize }}.
            </p>
          </div>
          <span class="chev">›</span>
        </a>
        {% endfor %}
      </div>
    </main>

    {% include 'global/footer.twig' %}
  </body>
</html>
